---
create_user:
  accountEnabled: "{{AZUserAccountEnabled}}"
  displayName: "{{AZUserDisplayName}}"
  mailNickname: "{{AZUserMailNickname}}"
  userPrincipalName: "{{AZUserPrincipalName}}"
  passwordProfile:
    forceChangePasswordNextSignIn: false
    password: "{{AZUserPassword}}"
